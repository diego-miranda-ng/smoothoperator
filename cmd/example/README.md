# Worker manager usage example

Run the example:

```bash
go run ./cmd/example
```

Press Ctrl+C (or send SIGTERM) to stop all workers and exit.

Build a binary:

```bash
go build -o example ./cmd/example
./example
```
